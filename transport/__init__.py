"""Этап 3 — транспорт-обёртка как переключаемые модули поверх ядра Этапа 2.

Ключевая идея (критерий ревью №2): все модули (a)/(b)/(c) реализуют ОДИН
контракт `Carrier` — сообщение-ориентированный канал для непрозрачных фреймов.
Noise-туннель (Этап 2) работает поверх любого carrier одинаково, поэтому на
Этапе 4 модули сравниваются честно: различается только то, КАК фреймы выглядят
на проводе, а не логика туннеля.

Модули:
  - (c) padding/fragmentation  -> transport.padding
  - (a) Reality-стиль (TLS)    -> transport.reality   (см. отдельный этап-ревью)
  - (b) QUIC/HTTP3             -> требует реального QUIC-стека (решение по зависимости)

Базовый «без обфускации» carrier (PlainTcp) — это эталон «голого» ядра для
сравнения в детекторе.
"""
from .base import Carrier, CarrierClient, CarrierServer, CostStats

__all__ = ["Carrier", "CarrierClient", "CarrierServer", "CostStats"]
