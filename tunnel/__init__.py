"""Экспериментальный транспорт-туннель (R&D, стенд).

Этап 2 — крипто-ядро. Обмен ключами и аутентификация сторон полностью на
аудированном Noise Protocol Framework (паттерн IK, noiseprotocol). Здесь НЕ
изобретается ни шифр, ни обмен ключами. Единственный написанный руками слой —
транспортный record для UDP (явный счётчик-nonce + окно защиты от повтора
поверх ChaCha20-Poly1305 из `cryptography`), по модели WireGuard. См.
stage2_crypto_core.md, раздел про TCP vs UDP record.
"""

NOISE_PROTOCOL = b"Noise_IK_25519_ChaChaPoly_SHA256"
# Контекст-строка прологуа: связывает хендшейк с этим проектом/версией, чтобы
# рукопожатие нельзя было переиграть в другом контексте. Обе стороны обязаны
# использовать идентичный prologue, иначе хендшейк не сойдётся.
DEFAULT_PROLOGUE = b"transport-obfuscator/stage2/noise-ik/v1"
