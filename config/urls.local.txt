# URL для LOOPBACK-смоука (локальный сайт из tools/localsite.py).
# И фон, и туннель ходят сюда по loopback -> захват на loopback-интерфейсе
# видит оба класса. НЕ добавляй сюда vk/google/свой-сервер: они на реальном
# NIC, на loopback их не видно, и пары не получится.
https://127.0.0.1:8443/
