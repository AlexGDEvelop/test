# Крипто-ядро и прототип. Крипто НЕ катаем руками — только аудированные либы.
noiseprotocol==0.3.1
cryptography>=42
# Этап 3, модуль (b): настоящий QUIC/HTTP3-стек (честный QUIC, не «похожий на»).
aioquic>=1.3
# Этап 4: детектор-эмулятор и фичи потока.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
dpkt>=1.9          # офлайн-парсинг pcap (без Npcap)
# Захват трафика на стенде — НЕ pip-пакет: установи Wireshark (даёт dumpcap/tshark
# + Npcap). scapy для live-захвата на Windows не используем (ненадёжно).
